//! > test detects operations


//! > cairo_code

fn main() {
    let x = 1;
    
    //!> operation can be simplified to zero
    let _ = 0 * x;  
    //!> operation can be simplified to zero
    let _ = 0 / x; 
    //!> operation can be simplified to zero
    let _ = x & 0;
}