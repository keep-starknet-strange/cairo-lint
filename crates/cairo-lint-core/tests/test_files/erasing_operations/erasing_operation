//! > Multiplication by zero

//! > cairo_code
fn main() {
    let x = 1;
    let y = 0 * x;
    let z = x * 0;
}

//! > diagnostics
warning: Plugin diagnostic: This operation always results in zero. This is likely not the intended outcome and should be corrected.
 --> lib.cairo:3:13
  |
3 |     let y = 0 * x;
  |             ^^^^^

warning: Plugin diagnostic: This operation always results in zero. This is likely not the intended outcome and should be corrected.
 --> lib.cairo:4:13
  |
4 |     let z = x * 0;
  |             ^^^^^

//! > fixed
fn main() {
    let x = 1;
    let y = 0;
    let z = 0;
}

//! > ==========================================================================

//! > Division by zero

//! > cairo_code
fn main() {
    let x = 1;
    let y = 0 / x;
}

//! > diagnostics
warning: Plugin diagnostic: This operation always results in zero. This is likely not the intended outcome and should be corrected.
 --> lib.cairo:3:13
  |
3 |     let y = 0 / x;
  |             ^^^^^

//! > fixed
fn main() {
    let x = 1;
    let y = 0;
}

//! > ==========================================================================

//! > Bitwise AND with zero

//! > cairo_code
fn main() {
    let x = 1;
    let y = x & 0;
    let z = 0 & x;
}

//! > diagnostics
warning: Plugin diagnostic: This operation always results in zero. This is likely not the intended outcome and should be corrected.
 --> lib.cairo:3:13
  |
3 |     let y = x & 0;
  |             ^^^^^

warning: Plugin diagnostic: This operation always results in zero. This is likely not the intended outcome and should be corrected.
 --> lib.cairo:4:13
  |
4 |     let z = 0 & x;
  |             ^^^^^

//! > fixed
fn main() {
    let x = 1;
    let y = 0;
    let z = 0;
}